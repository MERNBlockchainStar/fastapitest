import Layout from '../components/layout';
import DataList from '../components/datalist';

export const Home = () => {
    return (
        <Layout>
            <section id="home">
                <DataList />
            </section>
        </Layout>
    );
};

export default Home;
